# TWS Connection Settings
TWS_HOST=127.0.0.1
TWS_PORT=7496          # 7496 for live, 7497 for paper trading
TWS_CLIENT_ID=0        # Unique client ID (0-32)
TWS_TIMEOUT=30         # Request timeout in seconds

# MCP Server Settings
MCP_HOST=0.0.0.0       # Bind address (0.0.0.0 for all interfaces)
MCP_HTTP_PORT=8080     # HTTP port
